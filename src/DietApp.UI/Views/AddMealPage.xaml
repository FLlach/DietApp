<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:DietApp.UI.ViewModels"
             xmlns:models="clr-namespace:DietApp.UI.Models"
             xmlns:loc="clr-namespace:DietApp.UI.Localization"
             x:Class="DietApp.UI.Views.AddMealPage"
             x:DataType="vm:AddMealViewModel"
             x:Name="ThisPage"
             Title="{loc:Translate AddMeal_Title}">

    <!--
    Como funciona: Interfaz para armar y registrar una comida con uno o mas alimentos,
    especificando el gramaje exacto de cada uno para calcular los minerales resultantes.
    Por que se tomo esta decision: Permite al usuario componer un plato o ingesta completa
    en un solo paso antes de consolidar el conteo en el historial.
    -->

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="14">
            <Label Text="{loc:Translate AddMeal_Title}"
                   FontAttributes="Bold"
                   FontSize="18" />

            <!-- Encabezado de la comida -->
            <Border StrokeShape="RoundRectangle 10"
                    Padding="14"
                    Background="{AppThemeBinding Light=#FFFFFF, Dark=#25252D}">
                <Grid ColumnDefinitions="*, *" RowDefinitions="Auto, Auto" ColumnSpacing="10" RowSpacing="8">
                    <VerticalStackLayout Grid.Row="0" Grid.Column="0">
                        <Label Text="{loc:Translate AddMeal_Date}" FontAttributes="Bold" FontSize="12" />
                        <DatePicker Date="{Binding MealDate}" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Row="0" Grid.Column="1">
                        <Label Text="{loc:Translate AddMeal_Moment}" FontAttributes="Bold" FontSize="12" />
                        <Picker ItemsSource="{Binding MealTypeOptions}"
                                SelectedItem="{Binding SelectedMealTypeName}" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Row="1" Grid.ColumnSpan="2">
                        <Label Text="{loc:Translate AddMeal_Note}" FontAttributes="Bold" FontSize="12" />
                        <Entry Placeholder="{loc:Translate AddMeal_NotePlaceholder}"
                               Text="{Binding Note}" />
                    </VerticalStackLayout>
                </Grid>
            </Border>

            <!-- Seccion para agregar un alimento -->
            <Label Text="{loc:Translate AddMeal_AddFoodTitle}"
                   FontAttributes="Bold"
                   FontSize="15" />

            <Border StrokeShape="RoundRectangle 10"
                    Padding="14"
                    Background="{AppThemeBinding Light=#FFFFFF, Dark=#25252D}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="{loc:Translate AddMeal_SelectFood}" FontAttributes="Bold" FontSize="12" />
                    <Picker ItemsSource="{Binding AvailableFoods}"
                            ItemDisplayBinding="{Binding Name}"
                            SelectedItem="{Binding SelectedFood}"
                            Title="Toca para elegir alimento..." />

                    <Grid ColumnDefinitions="*, Auto" ColumnSpacing="10">
                        <VerticalStackLayout Grid.Column="0">
                            <Label Text="{loc:Translate AddMeal_GramsConsumed}" FontAttributes="Bold" FontSize="12" />
                            <Entry Placeholder="100"
                                   Keyboard="Numeric"
                                   Text="{Binding PortionGramsText}" />
                        </VerticalStackLayout>

                        <Button Grid.Column="1"
                                Text="{loc:Translate AddMeal_AddFoodButton}"
                                VerticalOptions="End"
                                Command="{Binding AddFoodToMealCommand}" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!-- Seccion para agregar una receta -->
            <Label Text="{loc:Translate AddMeal_AddRecipeTitle}"
                   FontAttributes="Bold"
                   FontSize="15" />

            <Border StrokeShape="RoundRectangle 10"
                    Padding="14"
                    Background="{AppThemeBinding Light=#FFFFFF, Dark=#25252D}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="{loc:Translate AddMeal_SelectRecipe}" FontAttributes="Bold" FontSize="12" />
                    <Picker ItemsSource="{Binding AvailableRecipes}"
                            ItemDisplayBinding="{Binding Title}"
                            SelectedItem="{Binding SelectedRecipe}"
                            Title="Toca para elegir receta..." />

                    <Grid ColumnDefinitions="*, Auto" ColumnSpacing="10">
                        <VerticalStackLayout Grid.Column="0">
                            <Label Text="{loc:Translate AddMeal_ServingsConsumed}" FontAttributes="Bold" FontSize="12" />
                            <Entry Placeholder="1"
                                   Keyboard="Numeric"
                                   Text="{Binding RecipeServingsText}" />
                        </VerticalStackLayout>

                        <Button Grid.Column="1"
                                Text="{loc:Translate AddMeal_AddRecipeButton}"
                                VerticalOptions="End"
                                BackgroundColor="#27AE60"
                                TextColor="White"
                                Command="{Binding AddRecipeToMealCommand}" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!-- Lista de Alimentos y Recetas Agregados a la Comida Actual -->
            <Label Text="{loc:Translate AddMeal_ItemsInMeal}"
                   FontAttributes="Bold"
                   FontSize="15" />

            <Border StrokeShape="RoundRectangle 10"
                    Padding="14"
                    Background="{AppThemeBinding Light=#FFFFFF, Dark=#25252D}">
                <VerticalStackLayout Spacing="8">
                    <VerticalStackLayout BindableLayout.ItemsSource="{Binding SelectedItems}"
                                         Spacing="6">
                        <BindableLayout.EmptyView>
                            <Label Text="{loc:Translate AddMeal_EmptyItems}"
                                   TextColor="Gray"
                                   HorizontalTextAlignment="Center"
                                   Padding="10" />
                        </BindableLayout.EmptyView>

                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="models:MealDraftItemModel">
                                <Grid ColumnDefinitions="*, Auto, Auto" ColumnSpacing="8" VerticalOptions="Center">
                                    <Label Grid.Column="0" Text="{Binding FoodName}" FontAttributes="Bold" VerticalOptions="Center" />
                                    <Label Grid.Column="1" Text="{Binding GramsDisplay}" TextColor="Gray" VerticalOptions="Center" />
                                    <Button Grid.Column="2"
                                            Text="Quitar"
                                            Command="{Binding Source={x:Reference ThisPage}, Path=BindingContext.RemoveItemCommand}"
                                            CommandParameter="{Binding .}"
                                            BackgroundColor="#E74C3C"
                                            TextColor="White"
                                            FontSize="11"
                                            Padding="6, 2" />
                                </Grid>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <!-- Mensaje de estado -->
            <Label Text="{Binding StatusMessage}"
                   FontAttributes="Bold"
                   HorizontalTextAlignment="Center"
                   TextColor="#2980B9" />

            <!-- Boton Guardar Comida -->
            <Button Text="{loc:Translate AddMeal_SaveButton}"
                    Command="{Binding SaveMealCommand}"
                    FontAttributes="Bold"
                    HeightRequest="48" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
